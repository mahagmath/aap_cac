# Metricbeat for Host & OpenSearch Metrics

This monitors:
- Host system metrics (CPU, memory, disk)
- OpenSearch metrics via HTTP module

## Run

```bash
docker-compose up
```

## Dashboards

Use OpenSearch Dashboards to visualize Metricbeat data under `metricbeat-*` index.
